# 0724 주차 알고리즘 변경 정리

브랜치 `0724_inprocess`. 실차 연습주행 관측을 바탕으로 한 개선이며, 아직
후진 단계까지 도달하는 녹화가 없어 후진 거동은 미검증 상태다. 다음 단계로
**line(주차선) 인식용 객체검출 모델을 개선**할 예정이다.

---

## 1. 대시보드에 슬롯 추적 상태 노출

`LockedSlotTracker`/ICP 추적 상태가 화면에 안 보여서 이후 튜닝 판단을 못 하던
문제. 라이브 대시보드에 한 줄 추가.

- `estimation/locked_slot.py`
  - `LockedSlotPose`에 `hold_scans: int` 추가 (몇 스캔째 hold 중인지).
  - 모션 게이트가 ICP 해를 거부할 때(`locked_slot_motion_gate_rejected`),
    이전엔 `translation_mm=0`으로 버려졌는데 이제 **실제 시도된 이동량/회전량**을
    실어서 반환. 게이트가 막은 게 진짜 튄 값인지, 임계가 너무 타이트한지 숫자로
    바로 판단 가능.
- `runtime/parking_app.py`
  - 대시보드에 `TRACK locked=Y held=Y(N) lost=N icp_d=..mm icp_rot=..deg (reason)` 표시.
    `--record-dashboard on`이면 그대로 영상에 찍힘.

## 2. 진입 후진 조향: 고정 → 연속 자세제어

`FOLLOW_ENTRY_CURVE`의 `lidar_box_curve` 진입에서 max 고정 조향을 heading 풀릴
때까지 그냥 유지하던 것을, **매 프레임 BEV 경로 곡률로 재계산**하도록 변경.
`reverse_entry_min_steering`(90) 바닥은 유지해 진입 초반엔 여전히 세게 꺾는다.

- `planning/t_parking_planner.py`
  - config 플래그 `reverse_entry_continuous_steering: bool = True` (기본 ON).
    `False`면 기존 고정 조향으로 롤백.
  - reason 문자열에 실제 조향값/모드 노출.

## 3. 단일차 LiDAR 슬롯 (2차 대기 없이 1대로 박스 생성)

3선(ㄷ자) 인식이 불안정하고 2차(gap) 확정이 자주 안 돼 후진 경로가 안 만들어지던
문제. 확정된 첫 차 1대만으로 슬롯 박스(규격 950×1500mm)를 만든다.

- `estimation/parking_lidar.py`
  - `single_car_slot_gap(first_car, config)`: 차의 LiDAR-facing 슬롯 모서리를
    한쪽 벽, 규격 폭만큼 진입 방향(-y_back)으로 떨어진 지점을 반대 벽, 깊이는
    차량-오른쪽(+x)으로. `choose_gap`과 동일 튜플을 반환해 기존 추적/투영 경로로 흐름.
  - config `single_car_slot_enabled: bool = False` (기본 OFF).
  - reason에 `single_car` 태그.
- `runtime/parking_app.py`: `--single-car-slot on/off` CLI.
- 방위 가정(+x 깊이)은 좌조향 시작 시점(차량이 도로와 거의 평행)에서만 유효.
  두 차가 보이면 즉시 재기준(reanchor)으로 덮어씀.
- **녹화 검증(방금 실패 영상)**: 슬롯 박스 안정적. 확정 스캔 35→491,
  깊이방향 뒤집힘 0회, min_consecutive_slot_depth_dot 0.829→0.999, width 정확히 950mm.
  단 이 녹화는 prealign에서 EMERGENCY_STOP이라 **후진 거동 자체는 미검증**.

## 4. 초반 rollout + base 조향 trim

- `start_forward_s`: 0.8 → **5.0**. 스페이스 후 5초간 검출 무시하고
  직진 → 옆 사람/초기 오검출을 첫 차로 오인하기 전에 지나칠 시간 확보.
- `straight_steering_trim`: -25 → **-35** (base 조향이 우측으로 소폭 쏠려 좌로 보정).
  음수=좌(prealign -150이 좌회전). 우측 쏠리면 더 음수로, 좌로 쏠리면 다시 올려서 튜닝.

## 5. first-car 사람 클러스터 배제

`select_first_approach_car`가 `center_x_right_mm >= 250`만 검사하고 포인트 수/선형성을
전혀 안 봐서, 사람(작고 산란한 클러스터)이 첫 차로 잡혀 턴을 오발시키던 구멍.

- `estimation/parking_lidar.py`
  - `is_first_car_eligible(cluster, config)`: 최소 포인트 수 + 표면 선형성 게이트.
    실물 차 표면(밀집·직선)만 첫 차로 인정.
  - config `first_car_min_points`, `first_car_min_linearity` (dataclass 기본 0/0.0 = 비활성).
    실제 임계는 `configs/parking.json`에만: `first_car_min_points: 6`, `first_car_min_linearity: 0.5`.
    (합성 최소 클러스터를 쓰는 단위 테스트 영향 방지)
- **검증 한계**: 방금 녹화에서 초반 클러스터가 6pts/0.5lin 필터를 **통과했다**
  (0~4s 35스캔 중 23스캔 first_car_confirmed=1). 즉 이 녹화에선 필터가 아니라 4초
  grace가 초반 검출을 미룬 것. 그 초반 클러스터가 실물 차인지 사람인지 이 데이터로는
  구분 불가. 라이브에서 사람 클러스터의 실제 pts/lin을 보고 임계 재조정 필요.

---

## 남은 작업 / 다음 단계

- **line(주차선) 인식 객체검출 모델 개선** (이번 세션 직후 착수 예정).
- option B(사람 배제) 튜닝을 위해 **대시보드에 후보 클러스터 pts/linearity 노출** 필요
  (현재 클러스터 개수만 표시).
- 후진 단계까지 도달하는 녹화로 3·2번(단일차 박스 경로추종 + 연속 조향) 실거동 검증.
- prealign 중 `lidar_safety_obstacle_during_prealign` EMERGENCY_STOP은 별개 이슈
  (풀 좌조향+빠른 전진이 꼬리를 옆차 안전 ROI에 넣음) — 미해결.

## 실행 명령어

```powershell
.venv\Scripts\python.exe scripts\parking.py `
  --source 1 --lidar-port COM5 --serial --serial-port COM6 `
  --camera `
  --single-car-slot on
```

연속 조향은 기본 ON. 단일차 슬롯은 `--single-car-slot on`. COM 포트는 실제 값으로.

## 주요 튜닝 노브 (`configs/parking.json`)

| 항목 | 값 | 의미 |
|---|---|---|
| `planner.start_forward_s` | 5.0 | 초반 blind 직진(검출 무시) 시간 |
| `planner.straight_steering_trim` | -35 | base 조향 오프셋(음수=좌) |
| `planner.reverse_entry_continuous_steering` | true(기본) | 진입 연속 조향, false면 고정 |
| `lidar.single_car_slot_enabled` | false | 1대만으로 슬롯 박스(실차 검증 후 on) |
| `lidar.first_car_min_points` | 6 | 첫 차 인정 최소 포인트(사람 배제) |
| `lidar.first_car_min_linearity` | 0.5 | 첫 차 인정 최소 표면 선형성(사람 배제) |
